{
  "design-queue": "// Implement Queue (factory/closure)  —  no LeetCode page\n//\n// Implement Queue() so calling it (WITHOUT `new`) returns an object with:\n//   count()  -> number of elements\n//   peek()   -> front value WITHOUT removing it; THROW if empty\n//   push(v)  -> add v to the back\n//   pop()    -> remove & return the front value; THROW if empty\n// Target: O(1) per operation. Hint: array.shift() is O(n) — use head/tail indexing.\n//\n// Fill in the body, then run:  node design-queue.js\n\nfunction Queue() {\n  // YOUR CODE HERE — remember to `return { count, peek, push, pop }`\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return Queue(...a); }\nconst CASES = [\n  { ops: [\"Queue\",\"push\",\"push\",\"peek\",\"pop\",\"count\",\"pop\"],\n    args: [[],[1],[2],[],[],[],[]],\n    expected: [null,null,null,1,1,1,2] },\n  { ops: [\"Queue\",\"pop\"], args: [[],[]], expected: [null,\"THROW\"] }\n];\nfunction runTests() {\n  let pass=0, fail=0, firstFail=null;\n  for (let c=0;c<CASES.length;c++){\n    const {ops,args,expected}=CASES[c];\n    let obj;\n    for (let i=0;i<ops.length;i++){\n      let got;\n      try { if(i===0){obj=build(args[i]);got=null;} else {const r=obj[ops[i]](...args[i]);got=(r===undefined?null:r);} }\n      catch(e){ got=\"THROW\"; }\n      if (JSON.stringify(got)===JSON.stringify(expected[i])) pass++;\n      else { fail++; if(!firstFail) firstFail=`case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail===0 ? `ALL PASS (${pass} assertions)` : `${fail} failed / ${pass+fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "queue-using-stacks": "// Implement Queue using Stacks (LeetCode 232)\n// Implement MyQueue using only two stacks (arrays w/ push/pop).\n// Methods: push(x) | pop() -> front | peek() -> front | empty() -> bool.\n// Amortized O(1) per op. Run: node queue-using-stacks.js\n\nclass MyQueue {\n  constructor() {\n    // YOUR CODE HERE (two stacks)\n  }\n  push(x) { /* YOUR CODE HERE */ }\n  pop() { /* YOUR CODE HERE -> return front */ }\n  peek() { /* YOUR CODE HERE -> return front */ }\n  empty() { /* YOUR CODE HERE -> return boolean */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new MyQueue(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"MyQueue\",\n      \"push\",\n      \"push\",\n      \"peek\",\n      \"pop\",\n      \"empty\"\n    ],\n    \"args\": [\n      [],\n      [\n        1\n      ],\n      [\n        2\n      ],\n      [],\n      [],\n      []\n    ],\n    \"expected\": [\n      null,\n      null,\n      null,\n      1,\n      1,\n      false\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "stack-using-queues": "// Implement Stack using Queues (LeetCode 225)\n// Implement MyStack using a queue (array w/ push + shift).\n// Methods: push(x) | pop() -> top | top() -> top | empty() -> bool.\n// Run: node stack-using-queues.js\n\nclass MyStack {\n  constructor() {\n    // YOUR CODE HERE (a single queue)\n  }\n  push(x) { /* YOUR CODE HERE (rotate so newest is at front) */ }\n  pop() { /* YOUR CODE HERE -> return top */ }\n  top() { /* YOUR CODE HERE -> return top */ }\n  empty() { /* YOUR CODE HERE -> return boolean */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new MyStack(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"MyStack\",\n      \"push\",\n      \"push\",\n      \"top\",\n      \"pop\",\n      \"empty\"\n    ],\n    \"args\": [\n      [],\n      [\n        1\n      ],\n      [\n        2\n      ],\n      [],\n      [],\n      []\n    ],\n    \"expected\": [\n      null,\n      null,\n      null,\n      2,\n      2,\n      false\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "min-stack": "// Min Stack (LeetCode 155)\n// Stack with push(x) | pop() (void) | top() -> val | getMin() -> val, ALL O(1).\n// Hint: track the running min alongside each element. Run: node min-stack.js\n\nclass MinStack {\n  constructor() {\n    // YOUR CODE HERE\n  }\n  push(x) { /* YOUR CODE HERE */ }\n  pop() { /* YOUR CODE HERE (void) */ }\n  top() { /* YOUR CODE HERE -> return top value */ }\n  getMin() { /* YOUR CODE HERE -> return current minimum, O(1) */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new MinStack(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"MinStack\",\n      \"push\",\n      \"push\",\n      \"push\",\n      \"getMin\",\n      \"pop\",\n      \"top\",\n      \"getMin\"\n    ],\n    \"args\": [\n      [],\n      [\n        -2\n      ],\n      [\n        0\n      ],\n      [\n        -3\n      ],\n      [],\n      [],\n      [],\n      []\n    ],\n    \"expected\": [\n      null,\n      null,\n      null,\n      null,\n      -3,\n      null,\n      0,\n      -2\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "design-circular-queue": "// Design Circular Queue (LeetCode 622)\n// MyCircularQueue(k): enQueue(v)->bool | deQueue()->bool | Front()->val|-1 |\n//   Rear()->val|-1 | isEmpty()->bool | isFull()->bool. Fixed capacity, O(1).\n// Hint: ring buffer with head index + count. Run: node design-circular-queue.js\n\nclass MyCircularQueue {\n  constructor(k) {\n    // YOUR CODE HERE (fixed array of size k, head, count)\n  }\n  enQueue(value) { /* YOUR CODE HERE -> false if full else true */ }\n  deQueue() { /* YOUR CODE HERE -> false if empty else true */ }\n  Front() { /* YOUR CODE HERE -> front value or -1 */ }\n  Rear() { /* YOUR CODE HERE -> rear value or -1 */ }\n  isEmpty() { /* YOUR CODE HERE */ }\n  isFull() { /* YOUR CODE HERE */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new MyCircularQueue(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"MyCircularQueue\",\n      \"enQueue\",\n      \"enQueue\",\n      \"enQueue\",\n      \"enQueue\",\n      \"Rear\",\n      \"isFull\",\n      \"deQueue\",\n      \"enQueue\",\n      \"Rear\"\n    ],\n    \"args\": [\n      [\n        3\n      ],\n      [\n        1\n      ],\n      [\n        2\n      ],\n      [\n        3\n      ],\n      [\n        4\n      ],\n      [],\n      [],\n      [],\n      [\n        4\n      ],\n      []\n    ],\n    \"expected\": [\n      null,\n      true,\n      true,\n      true,\n      false,\n      3,\n      true,\n      true,\n      true,\n      4\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "design-hashmap": "// Design HashMap (LeetCode 706)\n// Implement put(key,val) | get(key)->val|-1 | remove(key) WITHOUT a built-in Map.\n// Hint: array of buckets + a hash (key % size); chain collisions. Run: node design-hashmap.js\n\nclass MyHashMap {\n  constructor() {\n    // YOUR CODE HERE (array of buckets)\n  }\n  put(key, value) { /* YOUR CODE HERE (update if present, else append) */ }\n  get(key) { /* YOUR CODE HERE -> value or -1 */ }\n  remove(key) { /* YOUR CODE HERE */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new MyHashMap(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"MyHashMap\",\n      \"put\",\n      \"put\",\n      \"get\",\n      \"get\",\n      \"put\",\n      \"get\",\n      \"remove\",\n      \"get\"\n    ],\n    \"args\": [\n      [],\n      [\n        1,\n        1\n      ],\n      [\n        2,\n        2\n      ],\n      [\n        1\n      ],\n      [\n        3\n      ],\n      [\n        2,\n        1\n      ],\n      [\n        2\n      ],\n      [\n        2\n      ],\n      [\n        2\n      ]\n    ],\n    \"expected\": [\n      null,\n      null,\n      null,\n      1,\n      -1,\n      null,\n      1,\n      null,\n      -1\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "implement-trie": "// Implement Trie / Prefix Tree (LeetCode 208)\n// insert(word) | search(word)->bool (exact) | startsWith(prefix)->bool.\n// Hint: nested node objects; mark word ends. Run: node implement-trie.js\n\nclass Trie {\n  constructor() {\n    // YOUR CODE HERE (root node)\n  }\n  insert(word) { /* YOUR CODE HERE */ }\n  search(word) { /* YOUR CODE HERE -> exact word present? */ }\n  startsWith(prefix) { /* YOUR CODE HERE -> any word with this prefix? */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new Trie(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"Trie\",\n      \"insert\",\n      \"search\",\n      \"search\",\n      \"startsWith\",\n      \"insert\",\n      \"search\"\n    ],\n    \"args\": [\n      [],\n      [\n        \"apple\"\n      ],\n      [\n        \"apple\"\n      ],\n      [\n        \"app\"\n      ],\n      [\n        \"app\"\n      ],\n      [\n        \"app\"\n      ],\n      [\n        \"app\"\n      ]\n    ],\n    \"expected\": [\n      null,\n      null,\n      true,\n      false,\n      true,\n      null,\n      true\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "design-browser-history": "// Design Browser History (LeetCode 1472)\n// BrowserHistory(homepage): visit(url) (clears forward history) |\n//   back(steps)->url | forward(steps)->url (clamped). Run: node design-browser-history.js\n\nclass BrowserHistory {\n  constructor(homepage) {\n    // YOUR CODE HERE (history array + current index)\n  }\n  visit(url) { /* YOUR CODE HERE (truncate forward, then push) */ }\n  back(steps) { /* YOUR CODE HERE -> url after moving back, clamped to start */ }\n  forward(steps) { /* YOUR CODE HERE -> url after moving forward, clamped to end */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new BrowserHistory(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"BrowserHistory\",\n      \"visit\",\n      \"visit\",\n      \"visit\",\n      \"back\",\n      \"back\",\n      \"forward\",\n      \"visit\",\n      \"forward\",\n      \"back\",\n      \"back\"\n    ],\n    \"args\": [\n      [\n        \"leetcode.com\"\n      ],\n      [\n        \"google.com\"\n      ],\n      [\n        \"facebook.com\"\n      ],\n      [\n        \"youtube.com\"\n      ],\n      [\n        1\n      ],\n      [\n        1\n      ],\n      [\n        1\n      ],\n      [\n        \"linkedin.com\"\n      ],\n      [\n        2\n      ],\n      [\n        2\n      ],\n      [\n        7\n      ]\n    ],\n    \"expected\": [\n      null,\n      null,\n      null,\n      null,\n      \"facebook.com\",\n      \"google.com\",\n      \"facebook.com\",\n      null,\n      \"linkedin.com\",\n      \"google.com\",\n      \"leetcode.com\"\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "time-based-kv-store": "// Time Based Key-Value Store (LeetCode 981)\n// set(key,value,timestamp) | get(key,timestamp) -> value with the largest\n//   timestamp <= the query (or \"\" if none). Hint: binary search per key.\n// Run: node time-based-kv-store.js\n\nclass TimeMap {\n  constructor() {\n    // YOUR CODE HERE (key -> sorted list of [timestamp, value])\n  }\n  set(key, value, timestamp) { /* YOUR CODE HERE */ }\n  get(key, timestamp) { /* YOUR CODE HERE -> value for largest ts <= timestamp, else \"\" */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new TimeMap(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"TimeMap\",\n      \"set\",\n      \"get\",\n      \"get\",\n      \"set\",\n      \"get\",\n      \"get\"\n    ],\n    \"args\": [\n      [],\n      [\n        \"foo\",\n        \"bar\",\n        1\n      ],\n      [\n        \"foo\",\n        1\n      ],\n      [\n        \"foo\",\n        3\n      ],\n      [\n        \"foo\",\n        \"bar2\",\n        4\n      ],\n      [\n        \"foo\",\n        4\n      ],\n      [\n        \"foo\",\n        5\n      ]\n    ],\n    \"expected\": [\n      null,\n      null,\n      \"bar\",\n      \"bar\",\n      null,\n      \"bar2\",\n      \"bar2\"\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n",
  "lru-cache": "// LRU Cache (LeetCode 146) — the canonical senior design problem.\n// LRUCache(capacity): get(key)->val|-1 | put(key,val). Both O(1).\n// Interviewers want hashmap + doubly-linked list; a JS Map (insertion-ordered,\n// O(1) delete+set) achieves the same — be ready to explain either. Run: node lru-cache.js\n\nclass LRUCache {\n  constructor(capacity) {\n    // YOUR CODE HERE\n  }\n  get(key) { /* YOUR CODE HERE -> value or -1; mark as most-recently-used */ }\n  put(key, value) { /* YOUR CODE HERE -> insert/update; evict LRU if over capacity */ }\n}\n\n// ───── test harness (do not edit) ─────\nfunction build(a) { return new LRUCache(...a); }\nconst CASES = [\n  {\n    \"ops\": [\n      \"LRUCache\",\n      \"put\",\n      \"put\",\n      \"get\",\n      \"put\",\n      \"get\",\n      \"put\",\n      \"get\",\n      \"get\",\n      \"get\"\n    ],\n    \"args\": [\n      [\n        2\n      ],\n      [\n        1,\n        1\n      ],\n      [\n        2,\n        2\n      ],\n      [\n        1\n      ],\n      [\n        3,\n        3\n      ],\n      [\n        2\n      ],\n      [\n        4,\n        4\n      ],\n      [\n        1\n      ],\n      [\n        3\n      ],\n      [\n        4\n      ]\n    ],\n    \"expected\": [\n      null,\n      null,\n      null,\n      1,\n      null,\n      -1,\n      null,\n      -1,\n      3,\n      4\n    ]\n  }\n];\nfunction runTests() {\n  let pass = 0, fail = 0, firstFail = null;\n  for (let c = 0; c < CASES.length; c++) {\n    const { ops, args, expected } = CASES[c];\n    let obj;\n    for (let i = 0; i < ops.length; i++) {\n      let got;\n      try {\n        if (i === 0) { obj = build(args[i]); got = null; }\n        else { const r = obj[ops[i]](...args[i]); got = (r === undefined ? null : r); }\n      } catch (e) { got = \"THROW\"; }\n      if (JSON.stringify(got) === JSON.stringify(expected[i])) pass++;\n      else { fail++; if (!firstFail) firstFail = `case ${c} op#${i} ${ops[i]}(${JSON.stringify(args[i])}) -> got ${JSON.stringify(got)}, want ${JSON.stringify(expected[i])}`; }\n    }\n  }\n  console.log(fail === 0 ? `ALL PASS (${pass} assertions)` : `FAIL ${fail}/${pass + fail}\\n   First: ${firstFail}`);\n}\nrunTests();\n"
}
